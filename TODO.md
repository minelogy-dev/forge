# TODO

- **优化头文件导出编写难度**:目前公开头的「导出清单」散在多处且各
  维护一份——`function(build)` 的头同步循环(内联 os_listdir 过滤 .h)、
  `install_to` 的复制循环、以及 bootstrap 首次桥的 `cp lib/include/*.h`。
  新增一个公开头需要保证三处同步,极易漏。目标:单一导出描述
  (如 build.c 中一处声明头集合,install / build 同步 / 首次桥共用),
  从「三处清单」收敛为「一处来源」。

- **提供更方便的 string 封装**:运行时库内部已有 strv / strbuf,但
  公开面对构建脚本作者仍是裸 `string.h`(strcpy/strdup 手工内存管
  理)。目标:面向构建脚本的字符串 API——常见操作、统一错误处理
  (OOM 不静默)、明确的内存所有权约定,0 基础读者可直接使用而不
  必先学 C 字符串陷阱。

(本文件为内部工作清单,不随发布物英译。)